import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor = Color.fromRGBO(224, 224, 224, 1);
  static const Color focusedBorderColor = Color.fromRGBO(189, 189, 189, 1);
  static const Color fontColor = Color.fromARGB(255, 97, 97, 97);
  static const Color errorColor = Colors.redAccent;
  static const Color whiteColor = Colors.white;
  static const Color textBoxFillColor = Color.fromRGBO(238, 238, 238, 1);
  static const Color greyColor = Colors.grey;
  static const Color transparentColor = Colors.transparent;
  static const Color blueColor = Colors.blue;
  static const Color black = Colors.black;
}
